import AppLayout from '@/AppLayout';
import QuizzesPage from '@/QuizzesPage';

const QuizPage = () => {
  return (
    <AppLayout pageTitle="Quiz Builder">
      <QuizzesPage/>
    </AppLayout>
  );
};

export default QuizPage;
