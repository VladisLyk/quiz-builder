import AppLayout from "@/AppLayout";
import QuizCreatePage from "@/QuizCreatePage";

const Create = () => {
    return (
        <AppLayout pageTitle="Create quiz">
            <QuizCreatePage />
        </AppLayout>
    );
}

export default Create;